#include <stdio.h>

int main() {
    int C, D, temp;

    printf("Enter two numbers: ");
    scanf("%d %d", &C, &D);

    temp = C;
    C = D;
    D = temp;

    printf("After swapping:\nC = %d, D = %d", C, D);

    return 0;
}
